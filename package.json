{
  "name": "twilio_sms",
  "version": "0.2.0",
  "description": "A very basic twilio sms demo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "start:prod": "node index.js",
    "build": "cd client && npm install && npm run build",
    "client": "cd client && yarn start"
  },

  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^16.0.3",
    "express": "^4.16.4",
    "mongoose": "^5.7.5",
    "password-generator": "^2.2.0",
    "twilio": "^4.10.0"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
